@model ThuongMaiDienTu_DoAn.Models.SANPHAM
@{
    ViewBag.Title = "Đăng tin bán hàng";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-4">
    <h3 class="fw-bold mb-4 text-primary">📢 Đăng tin bán hàng</h3>

    @using (Html.BeginForm("TaoMoi", "SanPham", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        <div class="row bg-white shadow rounded p-4">

            <!-- CỘT TRÁI -->
            <div class="col-md-6">

                <label class="form-label fw-semibold">Tên sản phẩm</label>
                @Html.TextBoxFor(m => m.TenSP,
                    new { @class = "form-control mb-3", placeholder = "Nhập tên sản phẩm...", required = "required" })

                <label class="form-label fw-semibold">Danh mục</label>
                @Html.DropDownList("MaLoai", null,
                    "Chọn danh mục",
                    new { @class = "form-select mb-3", required = "required" })

                <label class="form-label fw-semibold">Giá bán (VNĐ)</label>
                <input name="Gia"
                       class="form-control mb-3 no-spin"
                       type="number" min="0"
                       placeholder="Ví dụ: 1500000" required />

                <label class="form-label fw-semibold">Số lượng</label>
                <input name="SoLuong"
                       class="form-control mb-3 no-spin"
                       type="number" min="1"
                       placeholder="Nhập số lượng" required />

                <!-- ẢNH BÌA -->
                <label class="form-label fw-semibold mt-2">Ảnh bìa sản phẩm</label>
                <input type="file" name="files" class="form-control mb-2" accept="image/*" required onchange="previewCover(this)" />

                <img id="previewCoverImg" class="mt-2 rounded d-none" style="width:160px;border:1px solid #eee;" />
            </div>

            <!-- CỘT PHẢI -->
            <div class="col-md-6">

                <label class="form-label fw-semibold">Mô tả chi tiết</label>
                @Html.TextAreaFor(m => m.MoTa, 6, 50, new { @class = "form-control mb-3", placeholder = "Mô tả tình trạng sản phẩm, phụ kiện đi kèm...", required = "required" })

                <!-- ẢNH CHI TIẾT -->
                <label class="form-label fw-semibold">Ảnh chi tiết sản phẩm (nhiều ảnh)</label>
                <input type="file" name="files" class="form-control" multiple accept="image/*" onchange="previewImages(this)" />

                <small class="text-muted">Có thể chọn nhiều ảnh cùng lúc.</small>

                <div id="previewList" class="mt-3 d-flex flex-wrap gap-2"></div>
            </div>

            <div class="col-12 mt-3">
                <button class="btn btn-warning px-4 py-2 fw-semibold">Đăng tin</button>
            </div>
        </div>
    }
</div>

<style>
    .no-spin::-webkit-inner-spin-button,
    .no-spin::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }
</style>

<script>
    // Preview ảnh bìa
    function previewCover(input) {
        if (input.files && input.files[0]) {
            const reader = new FileReader();
            reader.onload = function (e) {
                const img = document.getElementById("previewCoverImg");
                img.src = e.target.result;
                img.classList.remove("d-none");
            }
            reader.readAsDataURL(input.files[0]);
        }
    }

    // Preview nhiều ảnh chi tiết
    function previewImages(input) {
        const preview = document.getElementById("previewList");
        preview.innerHTML = "";

        [...input.files].forEach(file => {
            const reader = new FileReader();
            reader.onload = function (e) {
                const img = document.createElement("img");
                img.src = e.target.result;
                img.classList = "rounded border";
                img.style = "width:100px;height:100px;object-fit:cover;";
                preview.appendChild(img);
            }
            reader.readAsDataURL(file);
        });
    }
</script>
